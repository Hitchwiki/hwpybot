Want to add https://hitchwiki.org/en/Template:Coords-missing to articles without coordinates to promote them.

# Get started

create `user_password.py` from `user-password.py.example`

change bot name in `user-config.py`

run like this
```bash
python 2026_04_07_add_missing_coords_tag.py  
```