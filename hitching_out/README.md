Rename == Hitching out == section titles to == Hitchhiking out == & same for "in", this way we can access them better programatically.

# Get started

create `user_password.py` from `user-password.py.example`

change bot name in `user-config.py`

run like this
```bash
python 2026_04_10_rename_hitching_out.py  
```